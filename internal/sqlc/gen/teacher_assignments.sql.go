// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: teacher_assignments.sql

package queries

import (
	"context"
	"database/sql"
)

const listTeacherAssignmentsByTeacher = `-- name: ListTeacherAssignmentsByTeacher :many
SELECT ts.id,
       t.id   AS teacher_id,
       t.first_name,
       t.last_name,
       sub.id AS subject_id,
       sub.name AS subject_name,
       sec.id AS section_id,
       sec.name AS section_name
FROM teacher_subjects ts
JOIN teachers t ON t.id = ts.teacher_id
JOIN subjects sub ON sub.id = ts.subject_id
JOIN sections sec ON sec.id = ts.section_id
WHERE ts.teacher_id = ?
ORDER BY subject_name, section_name
LIMIT ? OFFSET ?
`

type ListTeacherAssignmentsByTeacherParams struct {
	TeacherID sql.NullString `json:"teacher_id"`
	Limit     int64          `json:"limit"`
	Offset    int64          `json:"offset"`
}

type ListTeacherAssignmentsByTeacherRow struct {
	ID          string         `json:"id"`
	TeacherID   string         `json:"teacher_id"`
	FirstName   sql.NullString `json:"first_name"`
	LastName    sql.NullString `json:"last_name"`
	SubjectID   string         `json:"subject_id"`
	SubjectName string         `json:"subject_name"`
	SectionID   string         `json:"section_id"`
	SectionName string         `json:"section_name"`
}

func (q *Queries) ListTeacherAssignmentsByTeacher(ctx context.Context, arg ListTeacherAssignmentsByTeacherParams) ([]ListTeacherAssignmentsByTeacherRow, error) {
	rows, err := q.db.QueryContext(ctx, listTeacherAssignmentsByTeacher, arg.TeacherID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeacherAssignmentsByTeacherRow
	for rows.Next() {
		var i ListTeacherAssignmentsByTeacherRow
		if err := rows.Scan(
			&i.ID,
			&i.TeacherID,
			&i.FirstName,
			&i.LastName,
			&i.SubjectID,
			&i.SubjectName,
			&i.SectionID,
			&i.SectionName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeacherAssignmentsByTenant = `-- name: ListTeacherAssignmentsByTenant :many
SELECT ts.id,
       t.id   AS teacher_id,
       t.first_name,
       t.last_name,
       sub.id AS subject_id,
       sub.name AS subject_name,
       sec.id AS section_id,
       sec.name AS section_name
FROM teacher_subjects ts
JOIN teachers t ON t.id = ts.teacher_id
JOIN subjects sub ON sub.id = ts.subject_id
JOIN sections sec ON sec.id = ts.section_id
WHERE t.tenant_id = ?
ORDER BY t.last_name, t.first_name, subject_name, section_name
LIMIT ? OFFSET ?
`

type ListTeacherAssignmentsByTenantParams struct {
	TenantID sql.NullString `json:"tenant_id"`
	Limit    int64          `json:"limit"`
	Offset   int64          `json:"offset"`
}

type ListTeacherAssignmentsByTenantRow struct {
	ID          string         `json:"id"`
	TeacherID   string         `json:"teacher_id"`
	FirstName   sql.NullString `json:"first_name"`
	LastName    sql.NullString `json:"last_name"`
	SubjectID   string         `json:"subject_id"`
	SubjectName string         `json:"subject_name"`
	SectionID   string         `json:"section_id"`
	SectionName string         `json:"section_name"`
}

func (q *Queries) ListTeacherAssignmentsByTenant(ctx context.Context, arg ListTeacherAssignmentsByTenantParams) ([]ListTeacherAssignmentsByTenantRow, error) {
	rows, err := q.db.QueryContext(ctx, listTeacherAssignmentsByTenant, arg.TenantID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeacherAssignmentsByTenantRow
	for rows.Next() {
		var i ListTeacherAssignmentsByTenantRow
		if err := rows.Scan(
			&i.ID,
			&i.TeacherID,
			&i.FirstName,
			&i.LastName,
			&i.SubjectID,
			&i.SubjectName,
			&i.SectionID,
			&i.SectionName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
