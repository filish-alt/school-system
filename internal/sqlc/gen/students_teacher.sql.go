// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: students_teacher.sql

package queries

import (
	"context"
	"database/sql"
)

const listStudentsByTeacher = `-- name: ListStudentsByTeacher :many
SELECT s.id, s.tenant_id, s.student_code, s.first_name, s.last_name, s.year, s.section_id, s.department_id, s.user_id, s.status
FROM students s
JOIN teacher_subjects ts ON ts.section_id = s.section_id
WHERE ts.teacher_id = ? ORDER BY s.last_name, s.first_name LIMIT ? OFFSET ?
`

type ListStudentsByTeacherParams struct {
	TeacherID sql.NullString `json:"teacher_id"`
	Limit     int64          `json:"limit"`
	Offset    int64          `json:"offset"`
}

func (q *Queries) ListStudentsByTeacher(ctx context.Context, arg ListStudentsByTeacherParams) ([]Student, error) {
	rows, err := q.db.QueryContext(ctx, listStudentsByTeacher, arg.TeacherID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Student
	for rows.Next() {
		var i Student
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.StudentCode,
			&i.FirstName,
			&i.LastName,
			&i.Year,
			&i.SectionID,
			&i.DepartmentID,
			&i.UserID,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
