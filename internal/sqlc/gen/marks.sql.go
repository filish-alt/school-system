// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: marks.sql

package queries

import (
	"context"
	"database/sql"
)

const getExamMarks = `-- name: GetExamMarks :many
SELECT 
    s.student_code, 
    s.first_name, 
    s.last_name, 
    sec.name as section_name, 
    es.total_score
FROM exam_sessions es
JOIN students s ON es.student_id = s.id
JOIN sections sec ON s.section_id = sec.id
WHERE es.exam_id = ? AND es.status IN ('submitted', 'timed_out')
ORDER BY s.last_name, s.first_name
`

type GetExamMarksRow struct {
	StudentCode sql.NullString `json:"student_code"`
	FirstName   sql.NullString `json:"first_name"`
	LastName    sql.NullString `json:"last_name"`
	SectionName string         `json:"section_name"`
	TotalScore  sql.NullInt64  `json:"total_score"`
}

func (q *Queries) GetExamMarks(ctx context.Context, examID sql.NullString) ([]GetExamMarksRow, error) {
	rows, err := q.db.QueryContext(ctx, getExamMarks, examID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetExamMarksRow
	for rows.Next() {
		var i GetExamMarksRow
		if err := rows.Scan(
			&i.StudentCode,
			&i.FirstName,
			&i.LastName,
			&i.SectionName,
			&i.TotalScore,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
